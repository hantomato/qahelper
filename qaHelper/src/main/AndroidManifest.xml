<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <application>

        <activity
            android:name=".act.OverlayPermissionActivity"
            android:theme="@style/QalMyTheme" />
        <activity
            android:name=".act.ScreenshotPreviewActivity"
            android:theme="@style/QalMyTheme" />
        <activity
            android:name=".act.CreateJiraActivity"
            android:theme="@style/QalMyTheme" />
        <activity
            android:name=".act.MediaProjectionPermissionActivity"
            android:theme="@style/QalMyTheme" />

        <service
            android:name=".service.QaOverlayService"
            android:enabled="true"
            android:exported="false" />

        <service
            android:name=".service.MediaProjectionService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />
    </application>
</manifest>